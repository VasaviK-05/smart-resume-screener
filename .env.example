# Copy this file to .env and fill in values.

# Optional. When missing or invalid, the engine uses deterministic rule-based scoring.
OPENAI_API_KEY=

# Chat model used for structured EvaluationSchema JSON.
OPENAI_MODEL=gpt-4o-mini

# Streamlit -> FastAPI base URL.
API_URL=http://127.0.0.1:8000
